# MCP Registry Metadata

Package: `garmin-mcp-unofficial`

MCP name: `io.github.davidmosiah/garminmcp`

Transport: stdio

Install:

```bash
npx -y garmin-mcp-unofficial
```

Setup:

```bash
npx -y garmin-mcp-unofficial setup
npx -y garmin-mcp-unofficial auth --install-helper
npx -y garmin-mcp-unofficial doctor
```
